let screenSize = Vector2.create(800, 600);

return {
    windowTitle = "Yeah",
    screenSize = screenSize,
    designResolution = screenSize * 0.5,
    fullscreen = false,
    graphicBackend = "opengl",
    enableGraphicAPIDebug = false,
    framesInFlight = 2,
    maxGeometryBufferVertices = null,
    enableStats = true
};