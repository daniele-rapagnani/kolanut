let screenSize = Vector2.create(400, 300);

return {
    windowTitle = "Yeah",
    screenSize = screenSize,
    designResolution = Vector2.create(400, 300),
    fullscreen = false,
    graphicBackend = "vulkan",
    enableGraphicAPIDebug = false,
    framesInFlight = 1,
    maxGeometryBufferVertices = null,
    enableStats = true
};